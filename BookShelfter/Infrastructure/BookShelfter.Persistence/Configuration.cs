using Microsoft.Extensions.Configuration;
using System.IO;

namespace BookShelfter.Persistence
{
    public static class Cococococoococ
    {
        //public static string ConnectionString
        //{
        //    get
        //    {
        //        ConfigurationManager configurationManager = new();
        //        var environment = Environment.GetEnvironmentVariable("ASPNETCORE_ENVIRONMENT");

        //        if (environment == "Production")
        //        {
        //            configurationManager.SetBasePath(Directory.GetCurrentDirectory());
        //            configurationManager.AddJsonFile("appsettings.Production.json", optional: false, reloadOnChange: true);
        //        }
        //        else
        //        {
        //            configurationManager.SetBasePath(Path.Combine(Directory.GetCurrentDirectory(), "../../Presentation/BookShelfter.API"));
        //            configurationManager.AddJsonFile("appsettings.development.json", optional: false, reloadOnChange: true);
        //        }

        //        return configurationManager.GetConnectionString("DefaultConnection");
        //    }
        //}
    }
}
